version: 2.1

orbs:
  architect: giantswarm/architect@1.0.0

workflows:
  test:
    jobs:
      - architect/go-test:
          name: go-test
          filters:
            # Trigger job also on git tag.
            tags:
              only: /^v.*/

      - architect/push-to-app-catalog:
          context: "architect"
          name: push-apptest-app-to-control-plane-catalog
          app_catalog: "control-plane-catalog"
          app_catalog_test: "control-plane-test-catalog"
          chart: "apptest-app"
          # Needed to trigger job also on git tag.
          filters:
            tags:
              only: /^v.*/
          requires:
            - go-test

      - architect/integration-test:
          name: "basic-integration-test"
          install-app-platform: true
          test-dir: "integration/test/examples/basic"
          filters:
            # Do not trigger the job on merge to master.
            branches:
              ignore:
                - master
          requires:
            - push-apptest-app-to-control-plane-catalog

      - architect/integration-test:
          name: "ensure-crds-integration-test"
          install-app-platform: true
          test-dir: "integration/test/examples/ensurecrds"
          filters:
            # Do not trigger the job on merge to master.
            branches:
              ignore:
                - master
          requires:
            - push-apptest-app-to-control-plane-catalog

      - architect/integration-test:
          name: "external-catalog-integration-test"
          install-app-platform: true
          test-dir: "integration/test/examples/externalcatalog"
          filters:
            # Do not trigger the job on merge to master.
            branches:
              ignore:
                - master
          requires:
            - push-apptest-app-to-control-plane-catalog

      - architect/integration-test:
          name: "remote-cluster-integration-test"
          install-app-platform: true
          test-dir: "integration/test/examples/remotecluster"
          filters:
            # Do not trigger the job on merge to master.
            branches:
              ignore:
                - master
          requires:
            - push-apptest-app-to-control-plane-catalog

      - architect/integration-test:
          name: "upgrade-app-integration-test"
          install-app-platform: true
          test-dir: "integration/test/examples/upgradeapp"
          filters:
            # Do not trigger the job on merge to master.
            branches:
              ignore:
                - master
          requires:
            - push-apptest-app-to-control-plane-catalog

